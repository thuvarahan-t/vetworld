spring.application.name=VetWorld
server.port=8080

# PostgreSQL Datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/vetworld_db
spring.datasource.username=postgres
spring.datasource.password=Vinayaga@05
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=vetworld-super-secret-key-for-jwt-authentication-2024
app.jwt.expiration=86400000

# Admin credentials (change in production!)
app.admin.username=admin
app.admin.password=vetworld2024
